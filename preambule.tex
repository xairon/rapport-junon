% ---------------------------------------------------------------------------
% Préambule — rapport de clôture JUNON
% Compilation : pdflatex + biber (voir Makefile)
% ---------------------------------------------------------------------------

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
\usepackage{textcomp}
\usepackage[french]{babel}
\usepackage{microtype}

% --- Mise en page ---------------------------------------------------------
\usepackage[a4paper,top=2.4cm,bottom=2.4cm,left=2.3cm,right=2.3cm,headsep=1em]{geometry}
\usepackage{setspace}
\setstretch{1.12}

% --- Couleurs -------------------------------------------------------------
\usepackage{xcolor}
\definecolor{bleuprofond}{HTML}{1F3A5F}
\definecolor{bleuclair}{HTML}{2E6DA4}
\definecolor{grisardoise}{HTML}{4A4A4A}
\definecolor{grisclair}{HTML}{F2F4F7}
\definecolor{ocre}{HTML}{B8860B}
\definecolor{vertsobre}{HTML}{2E7D5B}
\definecolor{rougesobre}{HTML}{A63A3A}

% --- Titres ---------------------------------------------------------------
\addtokomafont{disposition}{\color{bleuprofond}}
\addtokomafont{chapter}{\color{bleuprofond}}
\addtokomafont{section}{\color{bleuprofond}}
\addtokomafont{subsection}{\color{grisardoise}}
\setkomafont{caption}{\small}
\setkomafont{captionlabel}{\small\bfseries\color{bleuprofond}}

% Compacité des titres : KOMA réserve beaucoup d'espace en tête de chapitre.
\RedeclareSectionCommand[beforeskip=-1.2\baselineskip, afterskip=1.1\baselineskip]{chapter}
\RedeclareSectionCommand[beforeskip=-1.6ex plus -0.4ex minus -0.2ex,
                         afterskip=0.9ex plus 0.2ex]{section}
\RedeclareSectionCommand[beforeskip=-1.4ex plus -0.3ex minus -0.2ex,
                         afterskip=0.7ex plus 0.2ex]{subsection}

% --- En-têtes et pieds ----------------------------------------------------
\usepackage[automark,headsepline]{scrlayer-scrpage}
\pagestyle{scrheadings}
\clearpairofpagestyles
\ihead{\small\color{grisardoise}\headmark}
\ohead{\small\color{grisardoise}\thepage}
\setkomafont{pageheadfoot}{\normalfont\sffamily\small}

% --- Tableaux -------------------------------------------------------------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{longtable}
\usepackage{multirow}
\usepackage{array}
\newcolumntype{L}[1]{>{\raggedright\arraybackslash}p{#1}}
\newcolumntype{C}[1]{>{\centering\arraybackslash}p{#1}}
\newcolumntype{R}[1]{>{\raggedleft\arraybackslash}p{#1}}
\renewcommand{\arraystretch}{1.15}

% --- Figures et graphiques ------------------------------------------------
\usepackage{graphicx}
\usepackage{float}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,shapes.geometric,fit,backgrounds,calc}

% --- Encadrés -------------------------------------------------------------
\usepackage[most]{tcolorbox}

\newtcolorbox{encadre}[1][]{
  colback=grisclair,
  colframe=bleuclair,
  boxrule=0.4pt,
  leftrule=2.5pt,
  arc=1pt,
  left=7pt, right=7pt, top=4.5pt, bottom=4.5pt,
  fonttitle=\bfseries\color{white},
  #1
}

\newtcolorbox{limite}[1][]{
  colback=white,
  colframe=ocre,
  boxrule=0.4pt,
  leftrule=2.5pt,
  arc=1pt,
  left=7pt, right=7pt, top=4.5pt, bottom=4.5pt,
  fonttitle=\bfseries\color{white},
  #1
}

\newtcolorbox{resultat}[1][]{
  colback=white,
  colframe=vertsobre,
  boxrule=0.4pt,
  leftrule=2.5pt,
  arc=1pt,
  left=7pt, right=7pt, top=4.5pt, bottom=4.5pt,
  fonttitle=\bfseries\color{white},
  #1
}

% --- Question d'ouverture de chapitre -------------------------------------
\newcommand{\questionchapitre}[1]{%
  \begin{center}
  \begin{minipage}{0.88\textwidth}
    \color{bleuprofond}\itshape\large
    #1
  \end{minipage}
  \end{center}
  \vspace{0.3em}
}

% --- Bilan de fin de chapitre ---------------------------------------------
\newenvironment{acquisouvert}{%
  \par\vspace{0.7em}
  \noindent\textcolor{bleuclair}{\rule{\textwidth}{0.6pt}}
  \par\vspace{0.4em}
  \small
}{%
  \par\vspace{0.3em}
  \noindent\textcolor{bleuclair}{\rule{\textwidth}{0.6pt}}
  \par\vspace{1em}
}

% --- Listes ---------------------------------------------------------------
\usepackage{enumitem}
\setlist[itemize]{leftmargin=1.4em,itemsep=0.25em,topsep=0.4em}
\setlist[enumerate]{leftmargin=1.6em,itemsep=0.25em,topsep=0.4em}

% --- Unités et symboles ---------------------------------------------------
\usepackage{siunitx}
% Clés siunitx v3 : `locale` et `detect-all` (v2) sont évitées volontairement.
\sisetup{
  output-decimal-marker = {,},
  group-separator       = {\,},
  group-minimum-digits  = 4,
  range-phrase          = {\ à\ },
  range-units           = single,
}
% L'euro et l'année ne sont pas des unités siunitx : on les déclare.
\DeclareSIUnit{\euro}{\text{\texteuro}}
\DeclareSIUnit{\an}{an}

% --- Bibliographie --------------------------------------------------------
\usepackage[backend=biber,style=numeric,sorting=nyt,maxbibnames=6,giveninits=true]{biblatex}
\addbibresource{bibliographie.bib}

% --- Liens ----------------------------------------------------------------
\usepackage[hidelinks]{hyperref}
\hypersetup{
  colorlinks=true,
  linkcolor=bleuprofond,
  citecolor=bleuclair,
  urlcolor=bleuclair,
  pdftitle={Rapport de clôture — Programme JUNON},
  pdfauthor={Nicolas Ringuet},
}
\usepackage{url}
\urlstyle{same}

% --- Divers ---------------------------------------------------------------
\usepackage{csquotes}
\usepackage{caption}
\captionsetup{font=small,labelfont={bf,color=bleuprofond}}

% Commandes de raccourci ---------------------------------------------------
\newcommand{\code}[1]{\texttt{\small #1}}
\newcommand{\hubeau}{\code{hubeau\_data\_integration}}
\newcommand{\junon}{\code{time-serie-explo}}
